diff --git a/src/binary16/arch.rs b/src/binary16/arch.rs
index c7aefa8..dd713e3 100644
--- a/src/binary16/arch.rs
+++ b/src/binary16/arch.rs
@@ -710,6 +710,7 @@ pub(crate) const fn f16_to_f32_fallback(i: u16) -> f32 {
     let sign = half_sign << 16;
     // Unbias exponent
     let unbiased_exp = ((half_exp as i32) >> 10) - 15;
+    let man = (half_man & 0x03FFu32) << 13;
 
     // Check for subnormals, which will be normalized by adjusting exponent
     if half_exp == 0 {
@@ -717,14 +718,12 @@ pub(crate) const fn f16_to_f32_fallback(i: u16) -> f32 {
         let e = leading_zeros_u16(half_man as u16) - 6;
 
         // Rebias and adjust exponent
-        let exp = (127 - 15 - e) << 23;
-        let man = (half_man << (14 + e)) & 0x7F_FF_FFu32;
+        let exp = ((unbiased_exp + 127 - e as i32) << 23) as u32;
         return unsafe { mem::transmute::<u32, f32>(sign | exp | man) };
     }
 
     // Rebias exponent for a normalized normal
     let exp = ((unbiased_exp + 127) as u32) << 23;
-    let man = (half_man & 0x03FFu32) << 13;
     unsafe { mem::transmute::<u32, f32>(sign | exp | man) }
 }
 
